---
import "../styles/global.css";
import { AstroSeo } from "@astrolib/seo";

interface Props {
  title?: string;
  description?: string;
  image?: string; // Changed to just string
  canonical?: string;
  type?: "website" | "article";
}

const {
  title = "Joacim Eldre",
  description = "If this description shows, it means that Joacim forgot to set a description, boo! Hehe.",
  image = "/ogdefault.png", // Changed to public path
  canonical = Astro.url.href,
  type = "website",
} = Astro.props;

// Construct full URL for OG tags (they need absolute URLs)
const fullImageUrl = image.startsWith("http")
  ? image
  : new URL(image, Astro.site || Astro.url.origin).href;
---

<AstroSeo
  title={title}
  description={description}
  canonical={canonical}
  openGraph={{
    url: canonical,
    title: title,
    description: description,
    images: [
      {
        url: fullImageUrl,
        width: 1200,
        height: 630,
        alt: title,
        type: "image/png", // Changed to png
      },
    ],
    site_name: "Joacim Eldre",
    type: type,
    locale: "en_US",
  }}
/>

<meta charset="UTF-8" />
<meta name="viewport" content="width=device-width, initial-scale=1.0" />
<title>{title}</title>
<meta name="description" content={description} />

<!-- Favicons -->
<link rel="icon" type="image/png" href="/favicon-96x96.png" sizes="96x96" />
<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
<link rel="shortcut icon" href="/favicon.ico" />
<link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png" />

<!-- Canonical URL -->
<link rel="canonical" href={canonical} />

<!-- Additional SEO -->
<meta name="robots" content="index, follow" />
<meta name="googlebot" content="index, follow" />
<meta name="theme-color" content="#ffffff" />
<meta http-equiv="X-UA-Compatible" content="IE=edge" />

<script defer src="https://unpkg.com/alpinejs@3.x.x/dist/cdn.min.js"></script>
